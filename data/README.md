# 库存数据说明

## 📁 文件说明

### 1. `inventory.js`（推荐 - 使用本地图片）
- 使用 `require()` 引入本地图片
- 需要在 `/assets/images/` 目录下放置对应的图片文件
- 性能更好，离线可用

### 2. `inventory-no-images.js`（备选 - 使用网络图片）
- 使用网络图片 URL（Unsplash 示例图片）
- **无需准备本地图片，可直接运行**
- 需要网络连接

---

## 🚀 快速开始（推荐使用备选方案）

如果你想**快速测试应用**，不想准备本地图片，请按以下步骤：

### 步骤 1: 重命名文件
```bash
cd /Users/rexgu/Documents/Personal/Hackathon/data
mv inventory.js inventory-local.js
mv inventory-no-images.js inventory.js
```

或者直接删除 `inventory.js`，将 `inventory-no-images.js` 重命名为 `inventory.js`

### 步骤 2: 在代码中引入
```javascript
import inventory from '../data/inventory';
```

这样就可以直接运行了！

---

## 🖼️ 使用本地图片（可选）

如果你想使用本地图片版本，请按以下步骤：

### 步骤 1: 创建目录
在项目根目录创建 `/assets/images/` 文件夹：
```bash
mkdir -p assets/images
```

### 步骤 2: 准备图片
将以下图片放入 `/assets/images/` 目录：
- `teddy-bear.jpg` - 泰迪熊图片
- `lego-blocks.jpg` - 积木图片
- `kids-book.jpg` - 儿童书籍图片
- `desk-lamp.jpg` - 台灯图片
- `backpack.jpg` - 书包图片

**图片要求：**
- 格式：JPG 或 PNG
- 尺寸：建议 800x800px 左右
- 大小：建议每张 < 500KB

### 步骤 3: 确保使用正确的文件
确保 `inventory.js` 是使用 `require()` 的版本

---

## 📊 数据结构

每个物品包含以下字段：

```javascript
{
  id: '1',                    // 唯一标识
  name: '儿童玩具熊',         // 物品名称
  category: '玩具',           // 类别
  condition: '良好',          // 状态
  description: '...',         // 描述
  price: '免费',              // 价格
  suggestedPrice: '免费',     // 建议价格
  tags: ['玩具', '儿童'],     // 标签（用于搜索）
  image: require('...'),      // 本地图片（仅 inventory.js）
  imageUrl: 'https://...',    // 网络图片（仅 inventory-no-images.js）
  donorName: '李女士',        // 捐赠者
  location: '北京市朝阳区',   // 位置
  createdAt: '2024-01-15...'  // 创建时间
}
```

---

## 🔧 工具函数

库存文件提供了以下工具函数：

```javascript
import inventory, {
  getAllItems,          // 获取所有物品
  getItemById,          // 根据 ID 获取物品
  getItemsByCategory,   // 根据类别获取物品
  addItem,              // 添加新物品
  getAllCategories,     // 获取所有类别
} from '../data/inventory';
```

---

## ⚡ 我的建议

**第一次运行建议使用 `inventory-no-images.js`**，这样可以：
- ✅ 快速测试应用功能
- ✅ 无需准备本地图片
- ✅ 使用 Unsplash 高质量示例图片

等应用跑通后，再考虑切换到本地图片版本。
